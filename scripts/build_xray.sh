#!/bin/bash
# Xray binary is now downloaded at runtime by the app on first launch.
# This script exists for local testing only.
echo "Xray is downloaded on first app launch from GitHub releases."
echo "No need to bundle it."
